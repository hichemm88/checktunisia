# Expo / React Native
node_modules/
.expo/
dist/
web-build/
*.log

# Native
ios/
android/

# Env
.env*.local

# macOS
.DS_Store

# Generated
expo-env.d.ts

# Secrets — service-account keys must NEVER be committed (uploaded to EAS instead).
# NB: google-services.json is intentionally NOT ignored — it ships inside the APK.
*firebase-adminsdk*.json
google-play-service-account.json
*service-account*.json
